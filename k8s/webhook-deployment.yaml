apiVersion: apps/v1
kind: Deployment
metadata:
  name: crush-webhook
  namespace: crush
  labels:
    app: crush-webhook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crush-webhook
  template:
    metadata:
      labels:
        app: crush-webhook
    spec:
      serviceAccountName: crush-webhook
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: webhook
          image: YOUR_REGISTRY/crush-webhook:latest  # replace with your image
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: crush-config
            - secretRef:
                name: crush-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
