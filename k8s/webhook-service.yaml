apiVersion: v1
kind: Service
metadata:
  name: openhands-webhook
  namespace: openhands
  labels:
    app: openhands-webhook
spec:
  type: ClusterIP
  selector:
    app: openhands-webhook
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
